server:
  port: 8080

spring:
  profiles:
    active: local
  ai:
    dashscope:
      api-key: placeholder

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
  global-config:
    db-config:
      id-type: auto

logging:
  level:
    com.aichat: DEBUG
    org.springframework.ai: DEBUG

jwt:
  expiration: 86400000

app:
  upload-dir: ./uploads

tavily:
  api-key: placeholder
